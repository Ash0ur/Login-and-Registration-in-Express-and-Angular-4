# Login-and-Registration-in-Express-and-Angular-4

### Client

Run `npm install`
Run 'ng build'



### Server

Run `npm install`
add your MongoDB database Url in mongoose.connect() function in server.js
Run `node server'
